{
  "name": "question",
  "type": "module",
  "scripts": {
    "test": "jest --config jest.config.js",
    "test:coverage": "cross-env rm -rf ./coverage && jest --config jest.config.js --coverage",
    "view": "npm run test:coverage && open ./coverage/lcov-report/index.html"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "cross-env": "^7.0.3",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "5.2.0-dev.20230702"
  }
}
